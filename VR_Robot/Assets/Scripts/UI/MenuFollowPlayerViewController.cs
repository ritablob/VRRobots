using UnityEngine;

namespace UI
{
    public class MenuFollowPlayerViewController : MonoBehaviour
    {
        /* - menu circles around player
         * - menu is static
         * - get player face direction
         * - if player face too far from menu, slide menu towards player
         */


        // Start is called before the first frame update
        void Start()
        {
        }

        // Update is called once per frame
        void Update()
        {
        }
    }
}