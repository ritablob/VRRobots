using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InteractableFloat : MonoBehaviour
{
    /*
     * - get the input action reference for point / raycast to the object maybe?
     * - get player height
     * - distance where the object should be from the top of the player
     * - lerp te object upwards
     * - once no longer pointing at an object, lerp it downwards
     */
    // Start is called before the first frame update
    void Start()
    {
    }

    // Update is called once per frame
    void Update()
    {
    }
}